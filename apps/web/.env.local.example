# 本文件是模板，Next.js 不加载。请把真实值填到 .env.local（已 gitignore）。

# ── Google OAuth（Gemini 实验通道）──
# 运营方在 GCP 控制台创建 OAuth 2.0 Client ID（Web application）后填入此处。
# 不填则设置页的「Gemini · Google OAuth」选项会自动禁用。
# 记得把部署域名与 http://localhost:3001 加入该 Client 的 Authorized JavaScript origins。
NEXT_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=
# Gemini OAuth REST 示例通常还需要 x-goog-user-project 指定 quota project。
# 这不是密钥，但需要对应 GCP/IAM/quota 配置允许该请求使用此项目。
NEXT_PUBLIC_GOOGLE_CLOUD_PROJECT_ID=

# ── Supabase（账号托管）──
# 在 Supabase 项目 Settings → API 取 Project URL 与 anon/publishable key 填入。
# ⚠️ URL 必须是裸项目地址，不要带 /rest/v1/（supabase-js 会自己拼路径）。
# 不填则账号系统自动回退到本地模拟账号（localStorage）。
# 建议在 Supabase Auth 设置里关闭「Confirm email」以允许注册即登录（老人友好）。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
